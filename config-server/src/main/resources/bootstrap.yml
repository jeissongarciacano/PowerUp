server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri:
          username:
          password:
          default-label: main
          search-paths: config-data

  security:
    user:
      name: root
      password: secret
