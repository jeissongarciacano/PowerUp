server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeissongarciacano/PowerUp.git
          username: "jeissongarciacano"
          password: "bmx1999jeisson"
          default-label: main
          search-paths: config-data

  security:
    user:
      name: root
      password: s3cr3t
